<?php require('template/header.phtml'); ?>

    <div class="container mt-4">

        <h2 class="mb-4">Placement Opportunities</h2>

        <p class="text-muted">Welcome, <?= $_SESSION['name']; ?>!</p>


        <div class="row">
            <?php foreach ($placements as $placement) { ?>
                <div class="col-md-6 mb-4">
                    <div class="card shadow-sm">
                        <div class="card-body">

                            <h4 class="card-title"><?= $placement->getTitle(); ?></h4>
                            <p class="card-text"><?= $placement->getDescription(); ?></p>

                            <ul class="list-group list-group-flush mb-3">
                                <li class="list-group-item"><strong>Salary:</strong> £<?= $placement->getSalary(); ?></li>
                                <li class="list-group-item"><strong>Location:</strong> <?= $placement->getLocation(); ?></li>
                                <li class="list-group-item"><strong>Start:</strong> <?= $placement->getStartDate(); ?></li>
                                <li class="list-group-item"><strong>End:</strong> <?= $placement->getEndDate(); ?></li>
                            </ul>

                            <a href="placement_details.php?id=<?= $placement->getPlacementID(); ?>"
                               class="btn btn-primary w-100">
                                View Details
                            </a>

                        </div>
                    </div>
                </div>
            <?php } ?>
        </div>

    </div>

    </div>


<?php require('template/footer.phtml'); ?>
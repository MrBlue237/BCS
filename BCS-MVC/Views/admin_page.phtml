<!doctype html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport"
          content="width=device-width, initial-scale=1.0">
    <meta http-equiv="X-UA-Compatible" content="ie=edge">
    <title>Admin</title>

    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet">

</head>
<body>

<div class="container mt-4">

    <h3>Welcome,
        <?php echo htmlspecialchars($_SESSION['name']); ?>.

    </h3>

    <div class="row mt-4">

        <?php foreach ($view->placement as $placement) { ?>
            <div class="col-md-6 mb-4">
                <div class="card shadow-sm">
                    <div class="card-body">

                        <h4 class="card-title"><?= $placement->getUserID(); ?></h4>
                        <p class="card-text"><?= $placement->getRole(); ?></p>

                        <ul class="list-group list-group-flush mb-3">
                            <li class="list-group-item"><strong>Email:</strong> <?= $placement->getEmail(); ?></li>
                            <li class="list-group-item"><strong>Name:</strong> <?= $placement->getName(); ?></li>
                            <li class="list-group-item"><strong>Phone Number:</strong> <?= $placement->getPhoneNumber(); ?></li>
                            <li class="list-group-item"><strong>Address:</strong> <?= $placement->getPostalAddress(); ?></li>
                            <li class="list-group-item"><strong>CV:</strong> <?= $placement->getCvFilePath(); ?></li>
                        </ul>

                        <!-- Open modal button -->
                        <button type="button" class="btn btn-danger" data-bs-toggle="modal" data-bs-target="#deleteModal<?php echo $placement->getUserID(); ?>">
                            Delete User
                        </button>
                        <form action="edit_user.php" method="POST">
                            <button type="submit" class="btn btn-warning" name ="id" value="<?php echo $placement->getUserID() ?>">
                                Edit user details
                            </button>
                        </form>

                    </div>
                </div>
            </div>

            <!-- Modal -->
            <div class="modal fade" id="deleteModal<?php echo $placement->getUserID(); ?>" tabindex="-1" role="dialog">
                <div class="modal-dialog">
                    <form action="admin_page.php" method="POST" class="modal-content">
                        <div class="modal-header">
                            <h5 class="modal-title">Delete user </h5>
                            <button type="button" class="btn-close" data-bs-dismiss="modal" aria-label="Close"></button>
                        </div>
                        <div class="modal-body">
                            <p>Are you sure you want to delete this user</p>
                        </div>
                        <div class="modal-footer">
                            <button type="button" class="btn btn-secondary" data-bs-dismiss="modal">Cancel</button>
                            <button type="submit" class="btn btn-danger" name="delete_user" value="<?php echo $placement->getUserID()?>">Delete</button>
                        </div>
                    </form>
                </div>
            </div>

        <?php } ?>

    </div>
</div>

<script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/js/bootstrap.bundle.min.js"></script>

</body>
</html>

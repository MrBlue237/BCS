<?php require('template/header.phtml'); ?>

    <div class="row justify-content-center">
        <div class="col-md-6">
            <h2 class="mt-4 mb-3">Reset Your Password</h2>

            <?php if (!empty($view->error)): ?>
                <div class="alert alert-danger"><?php echo htmlspecialchars($view->error); ?></div>
            <?php endif; ?>

            <?php if (!empty($view->success)): ?>
                <div class="alert alert-success"><?php echo htmlspecialchars($view->success); ?></div>
            <?php else: ?>
                <form method="post" action="">
                    <!-- Hidden token to preserve it on POST -->
                    <input type="hidden" name="token" value="<?php echo htmlspecialchars($view->token); ?>">


                    <div class="mb-3">
                        <label for="password" class="form-label">New Password</label>
                        <input type="password" class="form-control" id="password" name="password" required>
                    </div>

                    <div class="mb-3">
                        <label for="confirm_password" class="form-label">Confirm New Password</label>
                        <input type="password" class="form-control" id="confirm_password" name="confirm_password" required>
                    </div>

                    <button type="submit" class="btn btn-primary w-100">Reset Password</button>
                </form>
            <?php endif; ?>
        </div>
    </div>

<?php require('template/footer.phtml'); ?>
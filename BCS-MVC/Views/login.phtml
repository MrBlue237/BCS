<?php require('template/header.phtml') ?>

    <div class="row justify-content-center">
        <div class="col-md-6">
            <h2 class="mt-4 mb-3">Login</h2>

            <?php
            if (isset($_SESSION['role']) && $_SESSION['logged_in'] === true):
                ?>
                <h2 class="mt-4 mb-3 text-center text-danger">Confirm Logout</h2>

                <div class="alert alert-warning text-center" role="alert">
                    You are currently logged in as **<?php echo htmlspecialchars($_SESSION['email']); ?>**.
                    <br>
                    <span class="fw-bold">Do you want to end your session?</span>
                </div>

                <div class="d-grid gap-2">
                    <a href="logout.php?confirm=true" class="btn btn-danger btn-lg">
                        Yes, Log Me Out
                    </a>
                    <a href="index.php" class="btn btn-secondary btn-lg">
                        Cancel
                    </a>
                </div>

            <?php
            else:
                ?>

                <?php if (!empty($view->loginError)): ?>
                <div class="alert alert-danger" role="alert">
                    <?php echo $view->loginError; ?>
                </div>
            <?php endif; ?>

                <form method="post" action="login.php">
                    <div class="mb-3">
                        <label for="email" class="form-label">Email</label>
                        <input type="email" class="form-control" id="email" name="email" required>
                    </div>
                    <div class="mb-3">
                        <label for="password" class="form-label">Password</label>
                        <input type="password" class="form-control" id="password" name="password" required>
                    </div>
                    <button type="submit" class="btn btn-primary btn-lg w-100">Login</button>
                </form>
                <p class="text-center">New user? <a href="register.php">Create account</a></p>

            <?php
            endif;
            ?>

        </div>
    </div>

<?php require('template/footer.phtml') ?>
<?php require('template/header.phtml') ?>

    <div class="row justify-content-center">
        <div class="col-md-6">
            <h2 class="mt-4 mb-3 text-center">Forgot Password</h2>

            <?php if (!empty($view->forgotError)): ?>
                <div class="alert alert-danger" role="alert">
                    <?php echo $view->forgotError; ?>
                </div>
            <?php endif; ?>

            <?php if (!empty($view->forgotSuccess)): ?>
                <div class="alert alert-success" role="alert">
                    <?php echo $view->forgotSuccess; ?>
                </div>
            <?php endif; ?>

            <form method="post" action="send_password.php">
                <div class="mb-3">
                    <label for="email" class="form-label fw-bold">Email</label>
                    <input
                            type="email"
                            class="form-control"
                            id="email"
                            name="email"
                            placeholder="name@example.com"
                            required>
                </div>

                <button type="submit" class="btn btn-primary btn-lg w-100">
                    Send Reset Link
                </button>
            </form>

            <p class="text-center mt-3">
                <a href="login.php">Back to Log in</a>
            </p>
        </div>
    </div>

<?php require('template/footer.phtml') ?>